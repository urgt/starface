PROD_URL=https://starface.pages.dev
ADMIN_USER=admin
ADMIN_PASSWORD=change-me

# Point this at an existing YuNet ONNX (reuse the one from scripts/models/).
YUNET_MODEL_PATH=../../models/yunet.onnx

# Optional: override the DINOv2 variant. Default is facebook/dinov2-large (1024-D).
# DINOV2_MODEL=facebook/dinov2-large

SEED_OUT_DIR=../../../seeds/wikidata
